<Window x:Class="WpfDotNet6WithFeatureFlags.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDotNet6WithFeatureFlags"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
    
        <StackPanel >
            <TextBlock Visibility="{Binding Path=FeatureAVisibility}">Feature A</TextBlock>
            <TextBlock Visibility="{Binding Path=FeatureBVisibility}">Feature B</TextBlock>
            <TextBlock Visibility="{Binding Path=FeatureCVisibility}">Feature C</TextBlock>
        </StackPanel>

    </Grid>
</Window>
